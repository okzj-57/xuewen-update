# xuewen-update

「学文 · 古汉语学习」安卓版的**更新发布点**。应用内的「检查更新」读这里的 `update.json`，
发现 `versionCode` 比当前大就下载对应的 APK，交给系统安装。

- `update.json` —— 版本信息（versionCode / versionName / 下载地址 / 更新说明）
- `xuewen-<版本>.apk` —— 安装包本体（同签名，可直接覆盖安装，学习进度保留）

这个仓库由应用作者维护，仅用于分发更新。
